# إعداد Supabase وAuth

## 1. تشغيل الـ Migration

افتح Supabase SQL Editor وشغّل محتوى `supabase.sql` كاملًا.

الجزء الأخير من الملف:

- يربط الحسابات بــ Supabase Auth.
- يحذف عمود كلمات المرور القديمة من `profiles`.
- يمنع الوصول غير الموثق إلى المحادثات والرسائل.
- ينشئ ملفًا آمنًا للمستخدم عند إنشاء حساب Auth.

إذا كانت هناك كلمات مرور قديمة مهمة، خذ نسخة احتياطية قبل تشغيل الـ Migration؛ بعد ذلك ستكون كلمات المرور مملوكة لـ Supabase Auth فقط.

## 2. إعداد أول مدير من التطبيق

أضف المتغير التالي إلى بيئة الخادم في Vercel أو `.env.local`:

```text
SUPABASE_SERVICE_ROLE_KEY=مفتاح_service_role_السري
```

بعد تشغيل التطبيق افتح `/setup` وأنشئ أول مدير مباشرة. بعد إنشاء المدير، تُغلق صفحة الإعداد تلقائيًا ولا تسمح بإنشاء مدير آخر.

بعد تسجيل دخول المدير، افتح «الإعدادات» ثم أضف أفراد العائلة من داخل التطبيق. لا يحتاج المدير إلى فتح Supabase Dashboard لإنشاء المستخدمين.

يستخدم التطبيق بريدًا داخليًا غير ظاهر للمستخدم مثل `ahmed@mychat.local` حتى يبقى تسجيل الدخول باسم المستخدم ممكنًا، بينما تبقى عملية إنشاء الحساب ومفتاح Service Role داخل الخادم فقط.

## 3. الصور

الصور لا تُحفظ في قاعدة البيانات أو `localStorage` أو Cache API. تبقى في ذاكرة الصفحة فقط أثناء فتح المحادثة، وتصل لحظيًا عبر Supabase Broadcast. إذا كان المستلم غير متصل وقت الإرسال فلن تظهر له لاحقًا. الحفظ الدائم يحدث فقط عندما يضغط المستخدم «تحميل على الجوال».
